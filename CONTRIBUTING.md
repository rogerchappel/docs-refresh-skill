# Contributing

Thank you for helping improve `docs-refresh-skill`.

## Local Checks

Use Node.js 20 or newer and run the release check before opening a pull request:

```sh
npm install
npm run release:check
```

Keep fixtures deterministic and local to the repository. Avoid examples that require network access, private documentation, or live credentials.
